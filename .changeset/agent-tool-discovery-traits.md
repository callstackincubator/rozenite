---
"rozenite": major
"@rozenite/agent-bridge": minor
"@rozenite/agent-sdk": minor
"@rozenite/agent-shared": minor
---

Expose globally qualified tool names, descriptions, and optional safety traits
in agent CLI tool discovery. Agent tool contracts now support `readOnly`,
`destructive`, and `idempotent` metadata.

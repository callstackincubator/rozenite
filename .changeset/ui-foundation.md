---
'@rozenite/ui': minor
---

Add new `@rozenite/ui` package: shared design tokens, a `Split` resizable
layout primitive, and `PluginShell` / `PluginHeader` layout components with
built-in light/dark theming for building DevTools plugin panels.

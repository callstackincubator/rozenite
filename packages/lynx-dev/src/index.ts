/**
 * @deprecated `@rozenite/lynx-dev` has been merged into `@rozenite/lynx`.
 * Install `@rozenite/lynx` instead and import from `@rozenite/lynx/rspeedy`.
 * This package is kept only as a re-export shim so existing
 * `import { rozeniteLynxPlugin } from '@rozenite/lynx-dev'` code keeps
 * working, and will be removed in a future major version — see issue #488
 * for the rationale.
 */
export * from '@rozenite/lynx/rspeedy';

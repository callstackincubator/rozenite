{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@rozenite/require-profiler-plugin",
  "targets": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["{projectRoot}/src/**/*"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}

{
  "name": "@rozenite/chrome-extension",
  "version": "0.0.0",
  "description": "Rozenite for Web",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.26",
    "@types/chrome": "^0.0.317",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "catalog:"
  },
  "engines": {
    "node": ">=20"
  }
}

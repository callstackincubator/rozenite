<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= panelName %> Panel</title>
    <style>
      html,
      body {
        height: 100%;
      }

      body {
        overflow: hidden;
      }

      #root {
        display: flex;
        height: 100%;
      }
    </style>
    <script>
      var __ROZENITE_PANEL__ = true;
    </script>
  </head>
  <body>
    <div id="root"></div>
    <script type="module">
      import { AppRegistry } from 'react-native';
      import PanelComponent from './panels/<%= panelFile %>';

      const panelName = '<%= panelFile %>';

      AppRegistry.registerComponent(panelName, () => PanelComponent);

      AppRegistry.runApplication(panelName, {
        initialProps: {},
        rootTag: document.getElementById('root'),
      });
    </script>
  </body>
</html>
